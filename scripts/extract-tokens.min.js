#!/usr/bin/env node

/**
 * Deprecated compatibility wrapper.
 * Kept to avoid breaking local aliases/scripts that still call this file.
 * Delegates to scripts/extract-tokens.js as the single source of truth.
 */

const { extractTokens } = require("./extract-tokens.js");

console.warn(
  "⚠️ scripts/extract-tokens.min.js is deprecated. Use scripts/extract-tokens.js instead.",
);

extractTokens();
